# ============================================================================
# MimicPERFormAF Preprocessing Configuration
# ============================================================================
#
# Purpose:
#   Convenient wrapper configuration for MimicPERFormAF dataset preprocessing.
#   Composes base_mimicperformaf_preprocessing and provides common overrides
#   for preprocessing MimicPERFormAF dataset files into HDF5 format suitable
#   for training.
#
# Inherits:
#   - base_mimicperformaf_preprocessing: Base MimicPERFormAF preprocessing configuration from ConfigStore
#
# Notes:
#   - Input and output file paths are required and must be provided via CLI
#   - This wrapper simplifies common preprocessing workflows for MimicPERFormAF datasets
#
# ============================================================================

defaults:
  - base_mimicperformaf_preprocessing
  - _self_

# Common field overrides for runtime configuration
input_file: ???  # Path to input .mat file (required)
output_file: ???  # Path to output directory or file (required)

# Usage Examples
# ============================================================================
# 1. Basic preprocessing:
#    python -m src.script.preprocess.preprocess \
#      preprocessor=mimicperformaf_preprocessing \
#      preprocessor.input_file=/path/to/input.mat \
#      preprocessor.output_file=/path/to/output.h5
#
# 2. Override additional preprocessing parameters:
#    python -m src.script.preprocess.preprocess \
#      preprocessor=mimicperformaf_preprocessing \
#      preprocessor.input_file=/path/to/input.mat \
#      preprocessor.output_file=/path/to/output.h5 \
#      preprocessor.save_files=true
# ============================================================================

